{
  "name":"talent-inspirations",
  "private":true,
  "scripts":{
    "lint":"npm --prefix client run lint",
    "build":"npm --prefix client run build",
    "server:start":"npm --prefix server start",
    "sync":"npm --prefix server run sync"
  }
}
